# NZH Web App (React)

This project runs with React using `react-scripts` (no Vite).

## Run locally

- `npm install`
- `npm run dev`

App runs at `http://localhost:3000`.

## Build

- `npm run build`

Build output is in `build/`.
